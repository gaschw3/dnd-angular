import { ArchetypeFilterPipe } from './archetype-filter.pipe';

describe('ArchetypeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ArchetypeFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
